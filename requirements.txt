# Pinned. Verified installing cleanly on Python 3.14.4; requires >= 3.10.
langgraph==1.2.11
langgraph-checkpoint-sqlite==3.1.1
langchain-anthropic==1.6.1
fastapi==0.141.1
uvicorn[standard]==0.52.4
jinja2==3.1.6
python-multipart==0.0.32
pydantic-settings==2.15.0
# Evidence is markdown, and the viewer renders it rather than showing the
# characters someone typed. Output is passed through app/render.py's allowlist
# before it reaches a page — markdown lets raw HTML through by design.
markdown==3.10

# Tests only
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1
